import { ReactElement } from "react";

import styles from "./features.module.css";

export default function FeaturesComponent(): ReactElement {
  return <section id="features" className={styles.features}></section>;
}
